{
  "name": "pulumi-app",
  "main": "index.ts",
  "scripts": {
    "deploy": "pulumi up -y -s dev",
    "destroy": "pulumi destroy -y"
  },
  "devDependencies": {
    "@types/node": "^16"
  },
  "dependencies": {
    "@pulumi/docker": "^4.4.0",
    "@pulumi/gcp": "^6.0.0",
    "@pulumi/pulumi": "^3.0.0"
  }
}
